// append the new object containing the gender of the owner with the cats array
function filteredObject(gender, catNames){
    let object = {
        "gender": gender,
        "catNames": catNames
    }
    return object;
}


